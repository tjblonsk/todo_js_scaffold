describe("A clone", function() {
  it("creates a an equivalent object", function() {
    todo1 = Object.create(todoItem);
    expect(todoItem).toEqual(todo1);
  });
});


describe("complete button", function(){
  it("returns a button", function(){
    button =
  }
    )
}